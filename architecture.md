# 🧬 КВАНТОВО-МАРМЕЛАДНАЯ АРХИТЕКТУРА СИСТЕМЫ (AI-KANE INFRA)

Все контейнеры развернуты в единой изолированной Docker-сети: `ai-shared-net`
Часовой пояс системы: `Europe/Ekaterinburg`

## 🐳 Список доступных микросервисов и эндпоинтов:

1. **ai_kane_agent (Наш Python-агент / Текущий шлюз)**
   - Внутренний порт: `8977`
   - Внешний порт (VS Code): `http://localhost:8977`
   - Назначение: Единая точка входа для Continue, интеграция контекста и личности.

2. **ai_ollama_core (Ядро Чат-модели Llama)**
   - Внутренний адрес: `http://ai_ollama_core:11434`
   - Внешний порт: `11435` (мапинг `11435:11434`)
   - Запущенная модель: `llama3.1:8b` (Чат, рефакторинг, изменения кода)
   - Модель эмбеддингов: `nomic-embed-text:latest`

3. **ai_qwen_core (Ядро Автодополнения Qwen)**
   - Внутренний адрес: `http://ai_qwen_core:11434`
   - Внешний порт: `11438` (мапинг `11438:11434`)
   - Запущенная модель: `qwen2.5-coder:1.5b` (Молниеносный Tab-complete)
   - Опции: `OLLAMA_NUM_PARALLEL=4`

4. **ai_vault_core (Хранилище секретов HashiCorp Vault)**
   - Внутренний адрес: `http://ai_vault_core:8200`
   - Внешний порт: `8200` (мапинг `8200:8200`)
   - Токен авторизации (`VAULT_DEV_ROOT_TOKEN_ID`): Считывается из `.env` (`${VAULT_MASTER_TOKEN}`)
   - Назначение: Безопасное хранение API-ключей, токенов баз данных.

5. **influxdb3_core (База данных временных рядов InfluxDB 3)**
   - Внутренний адрес: `http://influxdb3_core:8181`
   - Внешний порт: `8181` (мапинг `8181:8181`)
   - Назначение: Сбор метрик, логов работы агента, токенов автокомплита.

6. **grafana (Панель визуализации Grafana)**
   - Внутренний порт: `3000`
   - Внешний адрес: `http://localhost:3000`
   - Назначение: Построение графиков упругости кода и мониторинг ИИ.

7. **kokoro-tts (Синтез речи Kokoro Server)**
   - Внутренний адрес: `http://kokoro-tts:8880`
   - Внешний порт: `11436` (мапинг `11436:8880`)
   - Назначение: Генерация мармеладного голоса для котика.
